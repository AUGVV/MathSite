@page
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model IndexModel
@{
    ViewData["Title"] = "Profile";
    ViewData["ActivePage"] = ManageNavPages.Index;
}

<h4>@ViewData["Title"]</h4>
<partial name="_StatusMessage" model="Model.StatusMessage" />
<div class="row">
    <div class="col-md-6">
        <form id="profile-form" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Username"></label>
                <input asp-for="Username" class="form-control" disabled />
            </div>
            <div>@Localizer["Statistic"]</div><br>
            @Localizer["TasksSolved"] @ViewData["AnsweredCount"]<br>
            @Localizer["TasksCreated"] @ViewData["CreatedTasks"]<br>
            @Localizer["TasksVoted"] @ViewData["VotedCount"]<br>
            @Localizer["TasksRaiting"] @ViewData["ResultRaiting"]<br>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}